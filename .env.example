# Database path (inside container, persisted via docker volume)
DB_PATH=/app/data/data.db

# Shopping analytics API credentials (set via secret manager or runtime env)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
